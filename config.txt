# Audio player config — copy this file to the root of the SD card
# Keys are case-insensitive. Unknown keys are ignored.
#USB/Analog for default output.

defaultOutput = Analog
defaultVol    = 6
MaxVol        = 10
MinVol        = 1
